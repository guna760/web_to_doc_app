# URL to Document Converter

A lightweight Streamlit web application that scrapes visible text content from any provided website URL and packages it instantly into a downloadable Word (`.docx`) or PDF document.

## Setup Instructions

1. Ensure you have Python 3.11 installed.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt